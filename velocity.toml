
config-version = "2.7"

bind = "0.0.0.0:25565"

motd = "<gray>[<gradient:#3b82f6:#8b5cf6>KEYDAL</gradient><gray>] <white>Welcome"

show-max-players = 100

online-mode = false

force-key-authentication = false

prevent-client-proxy-connections = true

kick-existing-players = true

player-info-forwarding-mode = "modern"
forwarding-secret-file = "forwarding.secret"

announce-forge = false
ping-passthrough = "DISABLED"
sample-players-in-ping = false
enable-player-address-logging = true

[servers]
lobi = "127.0.0.1:25566"
sunucu = "127.0.0.1:25567"

try = [
    "lobi",
    "sunucu"
]

[forced-hosts]

[advanced]
compression-threshold = 256
compression-level = 6

login-ratelimit = 3000
command-rate-limit = 50
forward-commands-if-rate-limited = false
kick-after-rate-limited-commands = 10
tab-complete-rate-limit = 10
kick-after-rate-limited-tab-completes = 10

connection-timeout = 5000
read-timeout = 30000

haproxy-protocol = false

tcp-fast-open = true

bungee-plugin-message-channel = true

show-ping-requests = false

failover-on-unexpected-server-disconnect = true

announce-proxy-commands = true
log-command-executions = true
log-player-connections = true
accepts-transfers = false

enable-reuse-port = true

[query]
enabled = false
port = 25565
map = "kVelocity"
show-plugins = false
